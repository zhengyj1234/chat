nvm list available
nvm list
nvm current
nvm use

sk-5532b0769c4d434f8252296f1ef4d0db
sk-cbfkxiuqqxtddt0jjl93jhmflehnmtc3r53qjogyjjrhg177

react：
分写选 Vue/Angular，混写用 React
Vue / Angular：结构归结构，逻辑归逻辑

React：结构嵌套在 JS 里，JS 即页面

组件传值：
直接在页面定义里面 就像函数一样需要参数 才能到这个页面，此时父组件就可以传入值

1-2 登陆界面：
未实现部分：
实现部分：mock账号登陆，

系统Home
header完善：修改密码 功能取消
部门：成员名单/出勤人数
会议管理：
个人会议管理：

0521 breadcrumb 公式父路由实现
添加 加班管理

会议管理：
①去除  重置按钮 已有清除全部功能
②应该使整个页面填充完 不能留有太多空白
③搜索框 和 查询按钮 靠左
④存在bug 从。。和到。。如果是最后一天不显示
⑤删除<div className="timetable-date-sub">{d.slice(5)}</div> 下方没必要再显示一遍
⑥把这个页面的tsx文件顺需修改 从左到右 从上到下
⑦整体写的很乱，类似功能 定义 应该放在一起 整理一下
⑧没有check功能 要求：同一时间段 同一会议室 应该不能添加会议 

①使时间表 居中 显示
②显示今天和之后的六天 
③仍旧有bug 我可以添加 5-28的会议 添加完后 总会议数增加 但是 筛选后 仍旧没有 显示 可能是没有5-28？


ts：字典添加 字段 const newMeeting: Meeting = { ...meeting, id: ++meetingIdSeq };
const handleReset = () => {
        setKeyword('');
        setAppliedKeyword('');
        setFilterRooms([]);
        setFilterFrom('');
        setFilterTo('');
        setAppliedFilter({});
    };


会议操作履历
主页会议设置

0525
export const 和 function 区别：相当于定义了 一个固定函数 不能再被赋值。
函数提升：
function Page() {
  return <Dialog />
}

function Dialog() {
  return <Page />
}
实现 添加会议对话框拖动 
个人会议管理



①登陆注册功能
②添加会议功能


0528 副作用
hooks
useEffect()//
包括 watch mounted update  订阅 subscribe 和取消订阅 return

useRef页面不更新

①获取dom的工具
②存储任意值


登陆页面
功能实现：
	①使用了useactionstate（自动 ）  工号 密码 密码可视化
	useActionState：（1 好处 只有 input id => name 其余的empid password isloading 		error一个状态都不用定义 useActionState 全自动控制
			（2 disabled={isPending} 阻止二次登陆
	②登陆校验 调用 loginService接口 成功 迁移 到top 失败返回信息
	③登陆简单信息 存储到 localStorage，如果登陆 时长 大于 30分钟 重新登陆
主页面：
	①分块显示 <div header <div aside <div  breadcrumb main footer>> > replace 不		重新跳转
	②routes 实现 path element ={</>} - navigate `${xx.id}`
	③appbreadcrumb uselocation
会议管理
	①会议室管理
usecallback：{缓存 会变的 函数①函数被放进了 useEffect /useMemo 依赖数组②函数被传给了子组件（并且子组件做了性能优化 memo）}
usememo
	②
个人会议